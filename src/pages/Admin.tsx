import { Layout } from "@/components/site/Layout";

const Admin = () => (
  <Layout>
    <section className="container py-20 text-center">
      <h1 className="font-serif text-3xl text-primary-deep">Admin Dashboard</h1>
    </section>
  </Layout>
);

export default Admin;
